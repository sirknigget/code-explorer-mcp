Run the proof of concept with:

uv run python src/ts-parser-poc/run_poc.py

The Python wrapper bootstraps Node dependencies in this folder on first run, then invokes the ts-morph bridge and prints deterministic JSON for example.ts.
